package enums;

/**
  * The enum <code>WindowState</code> represents the different states fr the window
  * @version 1.0
  * @author Dorian Terbah 
**/
public enum WindowState {
    HOME_VIEW_STATE,
    IN_GAME_STATE,
    CREDIT_VIEW_STATE
}